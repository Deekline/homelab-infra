apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: d-kline-org-wildcard
  namespace: traefik
spec:
  secretName: d-kline-org-tls
  issuerRef:
    name: cloudflare-letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - "d-kline.org"
    - "*.d-kline.org"
