---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: speedtest-tracker-data-nfs
    namespace: speedtest-tracker
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: nfs-storage
    resources:
        requests:
            storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
    name: speedtest-tracker
    namespace: speedtest-tracker
    labels:
        app.kubernetes.io/name: speedtest-tracker
    annotations:
        reloader.stakater.com/auto: "true"
spec:
    replicas: 1
    selector:
        matchLabels:
            app.kubernetes.io/name: speedtest-tracker
    template:
        metadata:
            labels:
                app.kubernetes.io/name: speedtest-tracker
        spec:
            securityContext:
                fsGroup: 1000
            containers:
                - name: speedtest-tracker
                  image: lscr.io/linuxserver/speedtest-tracker:latest
                  imagePullPolicy: Always
                  ports:
                      - name: http
                        containerPort: 80
                        protocol: TCP
                  env:
                      - name: PUID
                        value: "1000"
                      - name: PGID
                        value: "1000"
                      - name: TZ
                        value: Europe/Warsaw
                      - name: DB_CONNECTION
                        value: pgsql
                      - name: DB_HOST
                        value: speedtest-tracker-db-rw
                      - name: DB_PORT
                        value: "5432"
                      - name: DB_DATABASE
                        value: speedtest_tracker
                      - name: DB_USERNAME
                        valueFrom:
                            secretKeyRef:
                                name: speedtest-tracker-db-credentials
                                key: username
                      - name: DB_PASSWORD
                        valueFrom:
                            secretKeyRef:
                                name: speedtest-tracker-db-credentials
                                key: password
                      - name: DISPLAY_TIMEZONE
                        value: Europe/Warsaw
                      - name: APP_URL
                        value: https://speedtest.d-kline.org
                      - name: APP_KEY
                        valueFrom:
                            secretKeyRef:
                                name: speedtest-tracker-secrets
                                key: APP_KEY
                  volumeMounts:
                      - name: data
                        mountPath: /config
                  resources:
                      requests:
                          cpu: 100m
                          memory: 256Mi
                      limits:
                          memory: 512Mi
            volumes:
                - name: data
                  persistentVolumeClaim:
                      claimName: speedtest-tracker-data-nfs

---
apiVersion: v1
kind: Service
metadata:
    name: speedtest-tracker
    namespace: speedtest-tracker
    labels:
        app.kubernetes.io/name: speedtest-tracker
spec:
    selector:
        app.kubernetes.io/name: speedtest-tracker
    ports:
        - name: http
          port: 80
          targetPort: http
